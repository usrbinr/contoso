% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dict.R
\name{contoso_dict_columns}
\alias{contoso_dict_columns}
\title{Data dictionary for the Contoso dataset}
\usage{
contoso_dict_columns()
}
\value{
A tibble with columns \code{table}, \code{rows} (row count of the whole
table), \code{column}, \code{type}, \code{constraints}, \code{units}, \code{missing}, \code{values} (a
list column of permitted values for enums) and \code{description}.
}
\description{
Reads the machine-readable data dictionary shipped with the package and
returns it as a tibble with one row per column of every table. The
dictionary follows the \href{https://data-dict.tidyverse.org/}{data-dict} schema
and documents the traps -- notably that \code{store_key} 999999 is a sentinel for
the online channel rather than a real location, and that \code{store.status} is
missing (never \code{"Open"}) for stores trading normally.
}
\examples{
contoso_dict_columns()

# one table
subset(contoso_dict_columns(), table == "store")

# which columns can be missing?
subset(contoso_dict_columns(), missing > 0, c(table, column, missing))
}
